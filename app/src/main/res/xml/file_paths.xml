<?xml version="1.0" encoding="utf-8"?>
<!-- Only exposes cacheDir/updates/, exactly where ApkDownloader saves a
     downloaded release APK, and nothing else in the app's storage. -->
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <cache-path name="updates" path="updates/" />
</paths>
